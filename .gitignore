# Secrets — this repo will hold a Google service account key and a deploy hook URL
.env
.env.*
!.env.example
*.pem
service-account*.json

# Dependencies
node_modules/

# Build output
.next/
.open-next/
out/
dist/
*.tsbuildinfo

# Cloudflare local state
.wrangler/
.dev.vars
.dev.vars.*

# Tool artifacts
.playwright-mcp/
*.log

# Impeccable working files (keep DESIGN.md, surfaces, sidecar)
.impeccable/build/
.impeccable/mocks/
.impeccable/review/
.impeccable/config.local.json

# macOS
.DS_Store
